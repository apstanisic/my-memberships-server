query {
    user(id: String!) {

    }
    companies
    company(id: String!)

}
type Query {
  user(id: String!): User!
  companies(page: Int, filter: String): [Company!]!
  company(id: String!): Company!
}