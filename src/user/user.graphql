
type User {
  id: ID!
  roles: [Role!]!
  subscriptions: [Subscription!]!
  email: String!
  name: String!
  avatar: String
  phoneNumber: String
}
  # ownedCompanies: [Company!]!
  # companiesWhereAdmin: [Company!]!

user {
  subscriptions {
    company
  }
}

company {
  subscriptions {
    user
  }

}

companies {
  subscriptions {
    user
  }
}
