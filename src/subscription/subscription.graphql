type Subscription {
  id: ID!
  company: Company!
  companyId: Company!
  owner: User!
  ownerId: User!
  startsAt: DateTime!
  expiresAt: DateTime!
  price: Int!
  allowedUses: String
  usedAmount: Int!
}
